Ini, TOML

[Paths]
target_directories = /tmp, /var/www/html
quarantine_dir = /var/quarantine_trap_scan
log_file = /var/log/trap_scan.log
scanned_cache_file = /var/log/trap_scan_cache.json

[Scanner]
suspicion_threshold = 2

[Scheduler]
# Alegeti metoda preferata: cron sau systemd. Lasa gol pentru a nu configura automat.
# scheduler_method =
# Frecventa de rulare (ex: hourly, daily, weekly, sau o expresie cron/systemd)
# scheduler_frequency =